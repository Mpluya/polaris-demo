apiVersion: carto.run/v1alpha1
kind: Workload
metadata:
  name: polaris-demo
  labels:
    apps.tanzu.vmware.com/workload-type: web
    app.kubernetes.io/part-of: polaris-demo
spec:
  params:
  - name: annotations
    value:
      autoscaling.knative.dev/minScale: "1"
  source:
    git:
      url: https://github.com/Mpluya/polaris-demo
      ref:
        branch: main
